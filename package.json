{
  "name": "planto-monorepo",
  "version": "1.0.0",
  "description": "PLANTO - Premium Smart Plant & Nursery Marketplace Monorepo",
  "scripts": {
    "install-all": "npm install --prefix client && npm install --prefix business && npm install --prefix admin && npm install --prefix backend",
    "client": "npm run dev --prefix client",
    "business": "npm run dev --prefix business",
    "admin": "npm run dev --prefix admin",
    "backend": "npm run dev --prefix backend",
    "dev": "concurrently \"npm run backend\" \"npm run client\" \"npm run business\" \"npm run admin\""
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
